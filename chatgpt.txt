🎯 PROMPT DEFINITIVO PARA EL AGENTE

Rol del agente
Actúa como Senior Frontend + Build Engineer especializado en Vite, React, TypeScript y Neon Auth.
Tu objetivo es lograr que npm run build pase en Vercel sin errores, sin introducir hacks, y siguiendo las APIs reales del proyecto.

📌 CONTEXTO OBLIGATORIO (NO ASUMIR NADA)

Proyecto: Vite + React + TypeScript

Hosting: Vercel

Auth: Neon Auth

Estado actual:
El build falla por errores de imports inexistentes / mal exportados, especialmente:

hooks que no existen

imports incorrectos desde client/src/services/api.ts

uso incorrecto de AuthView y CSS de Neon Auth

🚨 ERRORES QUE YA OCURRIERON (NO REPETIR)

❌ Importar api o apiClient sin verificar su export real

❌ Usar default import cuando el módulo no exporta default

❌ Importar AuthView desde @neondatabase/neon-js

❌ Importar CSS inexistente como
@neondatabase/neon-js/ui/tailwind.css

❌ Crear hooks que no coinciden con los endpoints reales del backend

❌ Dejar archivos importados que no existen físicamente

✅ TAREAS EXACTAS QUE DEBES REALIZAR (EN ESTE ORDEN)
1️⃣ VALIDAR EXPORTS REALES (CRÍTICO)

Abre y verifica literalmente:

client/src/services/api.ts


Debes confirmar qué se exporta realmente.
Solo puedes importar lo que esté explícitamente exportado.

👉 Si existe algo como:

export const apiService = { ... }


ENTONCES:

❌ NO usar import apiClient

❌ NO usar import api

✅ Usar apiService.getSensorReadings(...)

2️⃣ ARREGLAR useSensorReadings.ts (SIN INVENTAR)

Archivo esperado:

client/src/hooks/data/useSensorReadings.ts


Debe cumplir TODAS estas reglas:

✅ Importar solo desde exports reales

✅ Usar react-query

✅ Llamar al endpoint real:

GET /api/sensor-readings?deviceId=...


❌ NO hardcodear rutas /devices/:id/sensors

❌ NO usar axios directo si ya existe apiService

👉 Si el backend usa requireNeonAuth, el hook NO maneja auth.

3️⃣ VALIDAR EXISTENCIA DE ARCHIVOS IMPORTADOS

Antes de compilar:

Revisa cada import en:

Sensors.tsx

Analytics.tsx

Dashboard.tsx

Para cada import:

✅ El archivo existe

✅ El nombre coincide exactamente (case-sensitive)

✅ Exporta lo que se importa

Si un hook no existe, créalo.
Si no se usa, elimínalo del import.

4️⃣ CORREGIR NEON AUTH (ESTO ES CLAVE)
❌ PROHIBIDO
import { AuthView } from '@neondatabase/neon-js';
import '@neondatabase/neon-js/ui/tailwind.css';

✅ CORRECTO
import { AuthView } from '@neondatabase/neon-js/auth/react/ui';


📌 NO IMPORTES CSS DE NEON
Neon Auth no expone CSS público compatible con Vite.
El estilo debe resolverse con Tailwind del proyecto.

5️⃣ LIMPIEZA FINAL ANTES DE BUILD

Debes:

🔍 Buscar AuthView y verificar todos sus imports

🔍 Buscar apiClient, api, axios

🔍 Buscar imports rotos con rutas absolutas incorrectas

🔍 Eliminar código mock si rompe typings

6️⃣ VERIFICACIÓN FINAL (OBLIGATORIA)

Antes de terminar, ejecuta mentalmente:

npm run build


Y valida que:

❌ No hay errores Rollup

❌ No hay imports inexistentes

❌ No hay exports faltantes

❌ No hay CSS deep-import inválidos

✅ Vite termina correctamente

✅ esbuild genera api/index.js

🛑 REGLAS ABSOLUTAS

❌ NO inventes APIs

❌ NO asumas exports

❌ NO uses any

❌ NO agregues librerías nuevas

❌ NO cambies diseño visual

✅ SOLO correcciones estructurales y de build

✅ RESULTADO ESPERADO

Build exitoso en Vercel

Hooks coherentes con backend

Neon Auth funcionando sin imports rotos

Base sólida para continuar con telemetría + gráficos reales

Si quieres, en el próximo mensaje puedo:

🔧 Revisarte un archivo puntual

📊 Diseñar los hooks finales de Analytics

🚀 Prepararte el checklist pre-deploy definitivo para Vercel
