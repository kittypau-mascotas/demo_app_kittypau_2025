🐾 PUNTO 2 — STACK FRONTEND DEFINITIVO
ARQUITECTURA, PATRONES Y BASE TÉCNICA KITTYPAU (2026 READY)

Este stack no es “moda”, es decisión estratégica.

2.1 OBJETIVO DEL STACK FRONTEND

El frontend de KittyPau debe cumplir 4 objetivos críticos:

UX extremadamente fluida

Seguridad real (multi-tenant)

Escalabilidad sin refactor

Velocidad de desarrollo

Todo lo que no cumpla uno de estos → NO entra.

2.2 STACK TECNOLÓGICO FINAL (APROBADO)
🧱 Core
Capa	Tecnología	Motivo
Build Tool	Vite	Ultra rápido, simple
Framework	React 18	Ecosistema, madurez
Lenguaje	TypeScript	Seguridad total
Routing	React Router v6	Control explícito
Styling	Tailwind CSS	Velocidad + consistencia
UI Base	shadcn/ui	UI moderna, control total
Animaciones	Framer Motion	UX premium
Icons	lucide-react	Minimal + claro
🧠 Estado & Data
Uso	Librería
Server State	TanStack Query
Auth Session	Better Auth React
Local UI State	React hooks
Forms	React Hook Form + Zod
Charts	Recharts

👉 NO Redux
👉 NO Context gigante
👉 NO overengineering

2.3 ARQUITECTURA DE CARPETAS (CRÍTICO)

Esto es no negociable.

src/
├─ app/                  # Bootstrap de la app
│  ├─ App.tsx
│  ├─ router.tsx
│  └─ providers.tsx
│
├─ auth/                 # Todo lo relacionado a auth
│  ├─ auth-client.ts
│  ├─ use-session.ts
│  ├─ guards.tsx
│  └─ types.ts
│
├─ features/             # Feature-based architecture
│  ├─ pets/
│  │  ├─ components/
│  │  ├─ hooks/
│  │  ├─ services.ts
│  │  └─ types.ts
│  │
│  ├─ devices/
│  ├─ telemetry/
│  ├─ alerts/
│  └─ dashboard/
│
├─ components/           # UI reutilizable (NO lógica de negocio)
│  ├─ ui/                # shadcn
│  ├─ layout/
│  └─ feedback/
│
├─ services/             # Comunicación API
│  ├─ api.ts
│  ├─ http.ts
│  └─ errors.ts
│
├─ hooks/                # Hooks genéricos
│
├─ styles/               # tokens, themes
│
├─ utils/
│
└─ types/


👉 Regla de oro
Un feature no puede tocar otro feature directamente.

2.4 ROUTING: CONTROL TOTAL DE FLUJOS
Router centralizado
/              → redirect lógico
/login         → público
/register      → público
/onboarding    → protegido (parcial)
/dashboard     → protegido
/pets/:id      → protegido
/devices       → protegido
/settings      → protegido

Guards (clave)
<ProtectedRoute>
  <Dashboard />
</ProtectedRoute>


Tipos de guards:

AuthGuard

OnboardingGuard

RoleGuard (futuro)

👉 El router define la experiencia, no solo URLs.

2.5 AUTH FRONTEND (MEJOR IMPLEMENTACIÓN)
Cliente de auth
createAuthClient({
  baseURL: import.meta.env.VITE_API_URL,
  basePath: '/api/auth',
})

Hooks clave

useSession()

useLogin()

useLogout()

Estados manejados

loading

unauthenticated

authenticated

expired

👉 Nunca asumir sesión válida

2.6 COMUNICACIÓN CON API (ESTÁNDAR PROFESIONAL)
Cliente HTTP único
fetch(url, {
  credentials: 'include'
})


Características:

Cookies httpOnly

Manejo global de errores

Interceptor de 401

Retry controlado

Errores comunes:

401 → redirect login

403 → mensaje humano

500 → feedback elegante

2.7 DATA FETCHING (TANSTACK QUERY)
Principios

Cache first

Revalidación inteligente

Background refresh

Optimistic updates

Ejemplo:

useQuery({
  queryKey: ['pets'],
  queryFn: getPets,
  staleTime: 30_000,
})


👉 La UI nunca se bloquea

2.8 FORMULARIOS (UX PROFESIONAL)
Stack:

React Hook Form

Zod

Validación en tiempo real

Características:

Mensajes humanos

Validación inline

Disabled states claros

Ejemplo:

“Este nombre es demasiado corto para una mascota”

2.9 DISEÑO RESPONSIVE (MOBILE FIRST REAL)

KittyPau debe funcionar perfecto en celular.

Reglas:

Sidebar colapsable

Bottom navigation móvil

Cards stackeadas

Charts simplificados

👉 IoT = móvil primero.

2.10 PERFORMANCE (OBLIGATORIO)
Técnicas:

Lazy loading por feature

Code splitting

Memoización correcta

Imágenes optimizadas

Objetivo:

FCP < 1.5s

TTI < 2.5s

2.11 OBSERVABILIDAD FRONTEND

Desde el día 1:

Error boundaries

Logs de errores UI

Métricas de carga

👉 UX sin observabilidad no escala.

2.12 RESULTADO DEL PUNTO 2

Si esto se implementa:

✅ Frontend profesional nivel startup SaaS
✅ Seguridad frontend real
✅ UX fluida y confiable
✅ Código mantenible
✅ Escala sin reescritura


🐾 EPIC 2 — ARQUITECTURA FRONTEND
🎯 Objetivo

Frontend ordenado, mantenible y escalable.

🛠️ Tareas técnicas

2.1 Setup Proyecto

 Vite + React + TS

 ESLint + Prettier

 Alias de paths (@/)

2.2 Estructura de Carpetas

 features/

 services/

 auth/

 components/

 app/

2.3 Providers Globales

 QueryClientProvider

 AuthProvider

 ThemeProvider

2.4 Router

 Router centralizado

 Guards base

✅ DONE cuando: se puede agregar un feature nuevo sin tocar otros.

🐾 EPIC 2 — ARQUITECTURA FRONTEND
BASE TÉCNICA SÓLIDA, ESCALABLE Y SEGURA (KITTYPAU)

Objetivo del EPIC 2:
Definir cómo está construido el frontend por dentro, para que:

escale sin romperse

sea fácil de mantener

sea seguro (auth + ownership)

y se sienta profesional desde el primer commit

Este EPIC no es visual, es estructural.
Si aquí se hace mal → deuda técnica asegurada.

2.1 ESTRUCTURA DE CARPETAS (CANÓNICA)
🎯 Objetivo

Que cualquier dev entienda el proyecto en 2 minutos.

📁 ESTRUCTURA RECOMENDADA
src/
├── app/                 # Bootstrap de la app
│   ├── providers/       # Contexts globales
│   ├── router/          # Definición de rutas
│   ├── layouts/         # Layouts principales
│   └── guards/          # Route guards (auth)
│
├── features/            # Dominios funcionales
│   ├── auth/
│   ├── pets/
│   ├── devices/
│   ├── telemetry/
│   └── settings/
│
├── shared/              # Reutilizable global
│   ├── ui/              # Componentes base (Epic 1)
│   ├── hooks/
│   ├── lib/
│   ├── types/
│   └── utils/
│
├── services/            # API clients
│
├── styles/              # Tokens, themes
│
├── assets/              # Íconos, imágenes
│
├── main.tsx
└── App.tsx

✅ REGLAS CLAVE

❌ Nada de components/ genérico gigante

❌ Nada de imports cruzados caóticos

✅ Cada feature es autónoma

✅ shared solo depende de shared

2.2 FEATURE-BASED ARCHITECTURE (DDD LIGERO)
🎯 Objetivo

Que cada dominio viva en su propio mundo.

🧱 ESTRUCTURA INTERNA DE UNA FEATURE

Ejemplo features/pets/:

pets/
├── components/
├── hooks/
├── services.ts
├── types.ts
├── pages/
└── index.ts

📐 BENEFICIOS

Escala horizontal

Fácil testing

Fácil refactor

Menos bugs por acoplamiento

2.3 ROUTING STRATEGY (REACT ROUTER)
🎯 Objetivo

Control total del flujo de usuario.

🛠️ DEFINICIÓN DE RUTAS
/
├── /login
├── /register
├── /dashboard
│   ├── /pets
│   ├── /devices
│   ├── /analytics
│   └── /settings

📦 ARCHIVO CENTRAL
src/app/router/routes.ts


Declarativo

Tipado

Sin lógica de negocio

2.4 LAYOUTS (SEPARACIÓN VISUAL CLARA)
🎯 Objetivo

No repetir estructura visual.

🧱 LAYOUTS BASE
1️⃣ AuthLayout

Login

Register

Recover

👉 sin sidebar, sin ruido

2️⃣ AppLayout

Sidebar

Topbar

Content

📐 REGLAS

Las páginas no deciden layout

El router sí

2.5 AUTH FLOW (CRÍTICO)
🎯 Objetivo

Seguridad sin fricción.

🔐 ARQUITECTURA AUTH

Better Auth (frontend)

Neon Auth (backend)

Cookies httpOnly

Session en API

🛡️ Route Guards
<ProtectedRoute>
  <Dashboard />
</ProtectedRoute>


Reglas:

Session requerida

Redirección automática

Loading state elegante

2.6 PROVIDERS GLOBALES
🎯 Objetivo

Ordenar el caos global.

📦 PROVIDERS NECESARIOS
<AppProviders>
  <AuthProvider />
  <ThemeProvider />
  <QueryClientProvider />
  <Router />
</AppProviders>

⚠️ REGLAS

Providers solo en app/providers

Nada global fuera de aquí

2.7 DATA FETCHING (TANSTACK QUERY)
🎯 Objetivo

Datos consistentes, cacheados y seguros.

🧠 ESTRATEGIA

1 query = 1 endpoint

Keys bien definidas

Cache por usuario

🛠️ CONFIGURACIÓN BASE

retry controlado

staleTime razonable

error boundaries

2.8 API CLIENT (CAPA OBLIGATORIA)
🎯 Objetivo

Nunca llamar fetch directo desde componentes.

📦 services/api.ts

Responsabilidades:

baseURL

credentials

headers

manejo de errores

refresh de sesión

❌ PROHIBIDO
fetch('/api/devices')

✅ CORRECTO
devicesService.getAll()

2.9 ERROR HANDLING GLOBAL
🎯 Objetivo

Errores claros y humanos.

🛠️ IMPLEMENTACIÓN

ErrorBoundary global

Toasts UX friendly

Logs silenciosos

✅ DEFINITION OF DONE — EPIC 2

✔️ Arquitectura feature-based
✔️ Routing protegido
✔️ Auth centralizado
✔️ API client único
✔️ Providers ordenados
✔️ Sin imports caóticos
✔️ Base lista para escalar

2️⃣ ARQUITECTURA FRONTEND
🎯 Objetivo técnico

Código mantenible, predecible y escalable.

🛠️ Tareas

Implementar estructura app / features / shared

Configurar router centralizado

Crear layouts:

AuthLayout

AppLayout

Providers globales:

Auth

Theme

QueryClient

Guards de rutas

📦 Entregables

/app/providers

/app/router

/app/layouts

✅ DoD

Ningún componente llama fetch directo

Rutas protegidas correctamente

2️⃣ ARQUITECTURA FRONTEND (SESSION CORE)
🔹 Sesión actual
GET /api/auth/session


Respuesta

{
  "user": { "id": "...", "email": "..." },
  "activePetId": "..."
}

🔹 Logout
POST /api/auth/logout

2️⃣ SESIÓN / CONTEXTO
user_settings
import { boolean } from "drizzle-orm/pg-core";

export const userSettings = pgTable("user_settings", {
  userId: uuid("user_id").primaryKey().references(() => users.id),
  activePetId: uuid("active_pet_id"),
  theme: text("theme").default("light"),
  createdAt: timestamp("created_at").defaultNow(),
});

2️⃣ SESIÓN / CONTEXTO DE USUARIO
CREATE TABLE IF NOT EXISTS user_settings (
  user_id UUID PRIMARY KEY REFERENCES users(id) ON DELETE CASCADE,
  active_pet_id UUID,
  theme TEXT DEFAULT 'light',
  created_at TIMESTAMP DEFAULT now()
);

2️⃣ ARQUITECTURA API (BASE VERCEL)
2.1 Estructura real
api/
├── _middleware.ts
├── auth/
├── pets/
├── devices/
├── telemetry/
├── alerts/
├── dashboard/
└── user/

2.2 Middleware global (AUTH + OWNERSHIP)
export async function requireSession(req) {
  const session = await getSession(req);
  if (!session) throw new Unauthorized();
  return session.user;
}


✔️ Se ejecuta en TODAS las rutas protegidas
✔️ Nunca se salta